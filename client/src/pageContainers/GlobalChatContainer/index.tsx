import { GlobalChat } from '../../pages';

/* eslint-disable arrow-body-style */
const GlobalChatContainer = (): JSX.Element => {
	return (
		<GlobalChat />
	);
};

export default GlobalChatContainer;
